{
  "name": "clio-satellite",
  "version": "1.9.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=content.js --format=iife --platform=browser",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.0",
    "vitest": "^3.0.0"
  }
}
